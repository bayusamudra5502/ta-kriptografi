% \chapter{{{Pembuktian Jumlah Komunikasi pada Sinkronisasi Chaos Lin Tidak Konstan}}} \label{appendix:}

% Diberikan persamaan untuk melakukan sinkronisasi adalah persamaan \ref{eq:lin.henon.sync} dan \ref{eq:lin.henon.slave}. Definisi nilai error $e_i(k)$ ditunjukan oleh persamaan \ref{eq:lin.henon.error}.

% \begin{equation}
%   \label{eq:lin.henon.error}
%   e_i(k) = y_i(k) - x_i(k)
% \end{equation}

% Definisi dari matriks $E(k)$ ditunjukan oleh persamaan \ref{eq:lin.henon.matrixE}

% \begin{equation}
%   \label{eq:lin.henon.matrixE}
%   E(k) = \begin{bmatrix}
%     e_2(k) \\
%     e_3(k)
%     \end{bmatrix}
% \end{equation}

